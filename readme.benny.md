# readme

```bash
nvm install 18
nvm use 18
npm install

python3.11 -m venv .venv
source .venv/bin/activate
pip install tox

npm run build

pip install wheel
cd python

# TODO
export PYPI_TOKEN=''
rm -rf dist && bash deploy.sh
```
